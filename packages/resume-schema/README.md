# Resume Schema Package

This package is reserved for a JSON Resume-inspired schema shared by the API and web app.

The MVP schema lives in `apps/api/app/models/domain.py` while contracts are still moving quickly.
